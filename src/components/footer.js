import React from 'react'
import styled from 'styled-components'

const StyledFooter = styled.footer`
    background-color: teal;
    border-top: 1px solid var(--main-pop-color);
    height: 60px;
`

const Footer = () => {
    return (
        <StyledFooter>
            
        </StyledFooter>
    )
}

export default Footer
